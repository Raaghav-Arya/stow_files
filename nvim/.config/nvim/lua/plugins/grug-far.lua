return {
    "MagicDuck/grug-far.nvim",
    opts = {
        folding = {
            enabled = true,
            -- Include the file path line in the fold so you can fold at the file level
            include_file_path = true,
        }
    }
}
